# FireFlow OCR機能 Phase7(⑦実OCR API接続) 完了報告 2026-07-27

設計書5章⑦「OCR API接続」を実装した。Phase1(FSDF)〜Phase6(原紙プレビュー)で作った「差し込み口」に、実際のAIプロバイダ(Anthropic/OpenAI/Gemini)を接続し、設計書の①〜⑦全Phaseを完了させる、本OCR機能開発の最終Phaseである。

---

## 環境制約について(最初に必ずお読みください)

このセッションの作業環境には、以下の2つの重大な制約がある。**いずれも実装の手抜きではなく、サンドボックス環境そのものの制約**であり、隠さずここに明記する。

### 制約1: 実際のAI APIへのライブ接続検証ができない

この環境には実際の`ANTHROPIC_API_KEY`/`OPENAI_API_KEY`/`GEMINI_API_KEY`が無く、外部AI APIへのライブ通信もできない(ネットワークがブロックされている)。そのため、設計書5章⑦の完了条件である「**実際に撮影した紙の点検予定表の画像を使って、①〜⑥の全パイプラインが動作し、原紙プレビューで結果を目視確認できること。プロバイダをClaude→OpenAI→Geminiに設定だけで切り替えても同じ結果が得られること**」を、文字通りの意味(実画像・実APIキー・実ネットワーク)で本セッション内に検証することは物理的に不可能だった。

代わりに、本セッションで実行できる最大限の検証を行った。

- **`globalThis.fetch`をモックした単体テスト**(既存のAnthropicアダプタのテスト手法を踏襲)で、リクエストの組み立て・レスポンスのJSON抽出・エラーのラップ・APIキーの非漏洩を、Anthropic/OpenAI/Gemini全プロバイダで検証済み。
- **`TASK_ROUTING`の値を実際に書き換えて`resolveOcrCapability`を呼ぶテスト**で、「設定を変えるだけでプロバイダが切り替わる」という設計上の約束を機械的に確認済み(3プロバイダとも正しいAdapterが返ることを確認)。
- **ネットワークをモックしたNode契約テスト**で、サーバーAPIのレスポンス形状を模したJSON→`real_ocr_provider.js`→Normalizer→FSDF生成→バリデーションまでを一気通貫で確認済み(後述)。
- **Playwrightでのブラウザ実機テスト**(ネットワーク層をインターセプト)で、実際のブラウザ操作(ファイル選択→アップロード→確認画面表示、および認証エラー・ネットワークエラー時の表示)を確認済み。

これらは「コードが期待通りに組み立てられ、期待通りにレスポンスを解釈するか」を高い確度で保証するが、**「実際のAnthropic/OpenAI/Gemini APIが、本当にこの形式でリクエストを受け取り、期待通りのJSONを返すか」は未検証**である。特にOpenAI/Geminiは、公開されているAPI仕様の一般的な理解に基づくベストエフォート実装であり、一度も実APIへ疎通したことが無い。本番投入前に、実APIキーを設定した環境(本番/ステージング)での最終ライブ検証が別途必須である(詳細は本書末尾の「次Phase(実運用移行チェックリスト)」)。

### 制約2: `npm run typecheck`が、このセッションのサンドボックスでは完全にクリーンな状態にできない

このセッションには`node_modules`が一切インストールされておらず、`npm install`もnpmレジストリへの到達がブロックされているため実行できない(`403 Forbidden`)。グローバルにインストールされたTypeScript(6.0.3)・React・`@types/node`を極力かき集めて`node_modules`へシンボリックリンク/コピーし、`tsconfig.json`に`"types": ["node"]`と`"ignoreDeprecations": "6.0"`を追加することで、**Phase7で追加・変更した全ファイルについては型エラー0件**を確認できた。しかし、`next`パッケージ自体と`@types/react`はこの環境のどこにも存在せず入手できなかったため、**Phase7以前から存在する以下3ファイルで、`next/server`または`react`の型が解決できないという7件のエラーが残る**(いずれも今回のPhase7では1行も変更していないファイル)。

- `app/api/v1/health/route.ts`(`next/server`が見つからない)
- `app/layout.tsx`(`react`の型定義が見つからない。5件)
- `lib/http/errors.ts`(`next/server`が見つからない)

これらはPhase7の変更を一切加えていない状態でも同じエラーになることを確認済みであり(`next`パッケージそのものがこのサンドボックスに存在しないため、Phase1〜6を含むどのセッションでも`node_modules`が無い状態からは再現するはずの環境要因)、**Phase7のコード品質・正しさとは無関係の、サンドボックスのパッケージ入手性の問題**である。`npm install`が可能な通常の開発環境(ローカルPC・CI・Vercel)では、`package.json`通りに`next`・`react`・`@types/node`一式がインストールされるため、このエラーは発生しないはずである。念のため、ユーザー側の環境で`npm install && npm run typecheck`を実行し、0エラーになることを確認してほしい。

なお`tsconfig.json`への2つの追加(`types`・`ignoreDeprecations`)は、型チェックの意味・挙動を一切変えない(TypeScriptのバージョン差による表示上の警告を抑制し、`@types/node`の自動検出を明示化するだけの)設定であり、外部から見た挙動(API・プロンプト・エラーコード)には影響しない。

---

## 実装内容

### A. サーバー側(`live-board-nextjs/`)

#### A-1. 共通OCRコア(設計書1.1a)

- **`lib/ai/capabilities/ocr/core/commonExtractor.ts`(新規)**: サイズ上限をパラメータ化した`validateOcrPayload(mode, mediaType, data, limits)`、AM/PM・PASS・キャンセル・不在等の表記ゆれの扱いに関する共通プロンプト文言`COMMON_OCR_NOTATION_GUIDE`、`resolveOcrCapability(taskName).scan(...)`を呼ぶ薄いヘルパー`scanDocument(taskName, mode, mediaType, data, promptText, maxTokens)`を実装。

#### A-2. 帳票種別ごとの拡張モジュール(設計書1.1a)

- **`lib/ai/capabilities/ocr/documentTypes/residentTimeRequestSheet.ts`(新規)**: 既存の点検希望時間連絡票OCR(`inspectionSchedule.ts`)の中身をそのまま移設。**プロンプト文言(`SINGLE_PROMPT`/`BULK_PROMPT`)・`max_tokens`(400/16000)・サイズ上限(8MB/30MB)・エラーメッセージは1文字も変更していない**。サイズ検証だけ`core/commonExtractor.ts`の汎用版を呼ぶよう内部実装を整理した。
- **`lib/ai/capabilities/ocr/documentTypes/inspectionScheduleSheet.ts`(新規・本命)**: 「紙の点検予定表」用の新規プロンプトを実装。出力JSON形状は`lb_tool/ocr_intake/mock_ocr_provider.js`の`rawRooms`要素と完全に同じフィールド名(`roomNumberRaw`/`scheduleDayRaw`/`periodRaw`/`timeRaw`/`noteRaw`/`statusRaw`/`ladderRaw`/`memoRaw`/`confidence`)にした。`confidence`はモデルへ「0.0〜1.0で自己評価」させる指示を入れているが、**これは統計的にキャリブレーションされた確率ではなく、モデル自身の申告値に過ぎない参考値である**ことをコード内コメント・本報告書の両方に明記した。
  - サイズ上限は既存値(single 8MB / bulk 30MB)を流用(画像/PDFのバイト数上限であり、部屋の行数には連動しないため据え置いてよいと判断)。
  - `max_tokens`は既存(single 400 / bulk 16000)から引き上げ、**single 8000 / bulk 24000**とした。この帳票は1枚の画像でも複数部屋の行を含む表形式であり、出力がJSON配列(部屋数分)になるため既存の単一オブジェクト出力より大幅にトークン消費が増える見込みのため。実データでの実測はできていないため、次Phaseのチェックリストで要チューニングと明記した。
  - `mode:'single'`でも(1枚の画像に複数部屋の行が含まれる帳票のため)配列であることを、帳票種別固有の要件としてこのファイル内で追加検証している(providers/配下のAdapterはbulkモードのみ配列チェックを行う設計のため)。

#### A-3. 後方互換ラッパー

- **`lib/ai/capabilities/ocr/inspectionSchedule.ts`**: 中身を`documentTypes/residentTimeRequestSheet.ts`からのre-exportのみにした。既存の`lib/handlers/scanInspectionSchedule.ts`・既存テスト`test/unit/ai/capabilities/inspectionSchedule.test.ts`は**1文字も変更せず**そのまま全PASSすることを確認済み(後方互換性の証明)。

#### A-4. 型・タスクルーティング

- **`lib/ai/types.ts`**: `InspectionScheduleSheetOcrRoomResult`型を追加(既存`ScheduleOcrResult`等は無変更)。信頼度がキャリブレーションされた確率ではない旨をコメントに明記。
- **`lib/ai/config/taskRouting.ts`**: `TaskName`に`'ocr.inspectionScheduleSheet'`を追加、`ProviderName`に`'openai' | 'gemini'`を追加、`TASK_ROUTING`に`'ocr.inspectionScheduleSheet': 'anthropic'`を追加(既存の`'ocr.inspectionSchedule': 'anthropic'`は無変更)。
- **`lib/config/env.ts`**: `GEMINI_API_KEY: string | null`を追加(既存`OPENAI_API_KEY`同様、任意設定。未設定でも他機能に影響しない)。

#### A-5. OpenAI/Geminiプロバイダアダプタ(新規)

`providers/anthropic/`と同一構造(`client.ts`=低レベルHTTP呼び出し、`ocr.ts`=`OcrCapability`実装、`index.ts`=バレルエクスポート)で新設。

- **`lib/ai/providers/openai/{client.ts, ocr.ts, index.ts}`**: OpenAI Chat Completions API(`https://api.openai.com/v1/chat/completions`、モデル`gpt-4o`)を想定。画像は`image_url`ブロックに`data:<mediaType>;base64,<data>`形式のdata URIとして渡す。APIキーは`Authorization: Bearer`ヘッダのみに使用。
- **`lib/ai/providers/gemini/{client.ts, ocr.ts, index.ts}`**: Google Gemini `generateContent` API(`https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent`)を想定。画像/PDFは`inlineData: {mimeType, data}`形式で渡す。APIキーはURLクエリではなく`x-goog-api-key`ヘッダで送信(アクセスログへのキー露出を避けるため)。
- **`lib/ai/router.ts`**: `resolveOcrCapability`のswitch文に`'openai'`/`'gemini'`のcaseを追加。`never`型による網羅性チェックにより、対応漏れがあればコンパイルエラーになることを実装時に確認済み(一時的にcaseを外してエラーになることを確認してから戻した)。

#### A-6. Route Handler(新規エンドポイント)

- **`lib/handlers/scanInspectionScheduleSheet.ts`**・**`app/api/v1/inspection-schedule-sheet/scan/route.ts`**: 既存の`scanInspectionSchedule.ts`・`inspection-schedule/scan/route.ts`と全く同じ構造(`requireSession`→`enforceRateLimit('inspection-schedule-sheet.scan')`→`validateOcrPayload`→`scanInspectionScheduleSheet`→`okResponse`、`runtime='nodejs'`、`maxDuration=120`)。既存エンドポイントの挙動には一切影響しない、完全に独立した新規URL。

### B. クライアント側(`lb_tool/`、`live-board-nextjs/public/`に同期)

- **`lb_tool/ocr_intake/real_ocr_provider.js`(新規)**: `run_pipeline.js`の`ocrProvider`引数にそのまま渡せる、`/api/v1/inspection-schedule-sheet/scan`へfetchする実装。Node/ブラウザ両対応。認証ヘッダは既存の`fetchStampOcrResult`と同じ方式(`window.getAccessToken`があれば`Authorization: Bearer`で送る)。レスポンス(`{result: rawRooms}`)をmock_ocr_provider.jsと同じ`{sourceFile, rawRooms}`形へ変換し、配列であること・各要素がオブジェクトであることを防御的にチェックする。ネットワークエラー・認証エラー(401)・想定外レスポンス形状は、いずれもmockへ自動フォールバックせず、分かりやすい日本語メッセージの例外として呼び出し元へ伝播させる。
- **`lb_tool/index.html`統合**: 設定メニュー「点検予定表を読み取る」パネルに、「画像/PDFを選択してAIで読み取る」ボタン+ファイル入力を新設し、`real_ocr_provider.js`経由で実APIを呼ぶようにした。**既存のダミーサンプル選択(`#ocrScheduleScenarioSelect`/`#ocrScheduleRunBtn`)はPhase3の既存回帰テスト(`test_ocr_confirm_screen_phase3.js`)が直接操作するため、UI上に「動作確認用サンプル(オフラインでの画面確認向け)」として引き続き残した**(mockの結果と実データの結果が画面上で混同されないよう、見出しで明確に区別している)。実アップロードのエラーは専用のエラー表示欄(`#ocrScheduleUploadError`)に表示する。
- **`lb_tool/ocr_intake/test/real_ocr_provider_contract_test.js`(新規)**: サーバーレスポンス形状を模したJSON→`real_ocr_provider.js`→`normalizeInspectionScheduleSheet.js`→`fsdf_builder.js`→`validateFsdf.js`という実際のパイプラインを、`global.fetch`をモックして一気通貫で検証(14アサーション)。401エラー・ネットワークエラー・想定外レスポンス形状・ファイル未選択・トークン取得失敗時の各挙動も検証。**環境制約(実APIキー無し)に関係なくこのサンドボックスで実行・検証できる、Phase7で最も重要な統合テスト**と位置付けている。
- **`lb_tool/test_ocr_schedule_real_upload_phase7.js`(新規、Playwright)**: 実ブラウザ(Chromium)で、`page.route()`によるネットワーク層インターセプトを使い、(1)正常応答で確認画面が開くこと、(2)401応答で「認証エラー」を含む分かりやすいメッセージが表示され確認画面が開かないこと、(3)接続不能時に「ネットワークエラー」を含む分かりやすいメッセージが表示され、mockのダミー結果へ黙ってフォールバックしないこと、(4)アップロード失敗後も既存のダミーサンプル経路が独立して正常動作すること、を確認(11アサーション、全PASS)。
- **`demo_intake.html`**: 変更していない(引き続きmock_ocr_provider.jsを使う、オフライン動作確認用として維持)。
- **同期**: `lb_tool/index.html`・`lb_tool/ocr_intake/`配下を`live-board-nextjs/public/`へバイト単位で同期し、`test_ocr_confirm_sync.js`(17アサーション)で一致を確認済み。

---

## テスト結果

### TypeScript単体テスト(`npm run test:unit`、`live-board-nextjs/`配下)

全12ファイル、ALL PASS。

| テストファイル | 結果 |
|---|---|
| test/unit/ai/capabilities/inspectionSchedule.test.ts(既存・**無変更**) | PASS |
| test/unit/ai/capabilities/inspectionScheduleSheet.test.ts(新規) | PASS |
| test/unit/ai/providers/anthropic/client.test.ts(既存・無変更) | PASS |
| test/unit/ai/providers/openai/ocr.test.ts(新規) | PASS |
| test/unit/ai/providers/gemini/ocr.test.ts(新規) | PASS |
| test/unit/ai/router.test.ts(既存アサーション無変更+新規アサーション追加) | PASS |
| test/unit/cors.test.ts(既存・無変更) | PASS |
| test/unit/env.test.ts(既存・無変更) | PASS |
| test/unit/inviteService.test.ts(既存・無変更) | PASS |
| test/unit/noSecretsInPublic.test.ts(既存・無変更。新規追加ファイルにAPIキー等が含まれないことも再確認) | PASS |
| test/unit/rateLimit.test.ts(既存・無変更) | PASS |
| test/unit/verifySession.test.ts(既存・無変更) | PASS |

`npm run typecheck`は、Phase7で追加・変更した全ファイルに関しては0エラー。サンドボックスの`next`/`@types/react`未入手による7件の既存エラーのみ残る(前述「環境制約について」参照。Phase7の変更とは無関係)。

### 既存の全回帰テスト(JS)

| スイート | 件数 | 結果 |
|---|---|---|
| fireflow_fsdf/test/fsdf_roundtrip_test.js(変更なし) | 14 | PASS |
| lb_tool/ocr_intake/test/*(既存51件+新規real_ocr_provider_contract_test.js 14件) | 65 | PASS |
| lb_tool/ocr_confirm/test/*(変更なし) | 125 | PASS |
| fireflow_template_engine/test/*(10ファイル、変更なし) | 276 | PASS |
| rf_tool/test/*(変更なし) | 131 | PASS |
| lb_tool/test_ocr_confirm_sync.js(index.html本体+ocr_intake/ocr_confirm/fireflow_fsdfの同期) | 17 | PASS |
| **小計(数値集計分)** | **628** | **全PASS** |

加えて、数値集計に含めていないPlaywright E2Eテストも全PASS。

- `lb_tool/test_ocr_confirm_screen_phase3.js`(既存・無変更): 全PASS。実アップロードUI追加後も、Phase3完了条件(mock OCR確認画面の一連の操作)が壊れていないことを確認。
- `lb_tool/test_ocr_preview_screen_phase6.js`(既存・無変更): 全PASS。原紙プレビュー機能に影響がないことを確認。
- `lb_tool/test_ocr_schedule_real_upload_phase7.js`(新規): 11アサーション全PASS(前述)。
- `rf_tool/test_full_pipeline.js`系のe2eテスト(`e2e_fudai_remark_fix_test.js`・`e2e_fudai_unhandled_warning_test.js`・`e2e_validate_smoke.js`): 全PASS(変更なし)。

### `resolveOcrCapability`の網羅性テスト

`test/unit/ai/router.test.ts`に、`TASK_ROUTING['ocr.inspectionScheduleSheet']`を一時的に`'anthropic'`/`'openai'`/`'gemini'`へ書き換え、それぞれ正しいAdapter(`anthropicOcrCapability`/`openaiOcrCapability`/`geminiOcrCapability`)が返ることを確認するテストを追加した(テスト終了時に元の設定へ復元することも確認済み)。

---

## 既存機能への影響

- **点検希望時間連絡票OCR**(`lib/ai/capabilities/ocr/inspectionSchedule.ts`・`lib/handlers/scanInspectionSchedule.ts`・`app/api/v1/inspection-schedule/scan/route.ts`・`app/api/scan-time-request/route.ts`): URL・レスポンス形状・プロンプト文言・エラーコードは一切変更していない。内部実装のみ`documentTypes/residentTimeRequestSheet.ts`への委譲に整理した。既存テストは1文字も変更せず全PASS。
- **`rf_tool/`・`fireflow_fsdf/`・`fireflow_template_engine/`・`lb_tool/ocr_intake/`(mock_ocr_provider.js・normalize/・fsdf_builder.js・run_pipeline.js)・`lb_tool/ocr_confirm/`**: 一切変更していない。既存回帰テストが引き続き全PASSであることを確認済み。
- **`lb_tool/index.html`**: 「点検予定表を読み取る」パネル内にのみ変更(実アップロードUIの追加)。既存のstamp OCR画面(`fetchStampOcrResult`等)・部屋カード一覧・その他の画面には一切手を加えておらず、Phase3のPlaywright回帰テストで無影響を確認済み。
- **`live-board-nextjs/public/`**: `index.html`と`ocr_intake/`配下(新規2ファイル)を同期。他の`public/`配下ファイルは無変更。

---

## 未対応事項(正直に明記)

1. **【最重要・冒頭にも記載】実際のAnthropic/OpenAI/Gemini APIへのライブ接続検証は本セッションでは実施不可能**(実APIキー無し・外部ネットワーク到達不可)。全てfetchモック/ネットワークインターセプトによる検証にとどまる。ユーザー側で実際のAPIキーを設定した環境での最終検証が必須。
2. **OpenAI/Geminiプロバイダはベストエフォート実装(ライブ未検証)**。特にOpenAIの`image_url`コンテンツブロックは一般的な画像形式(JPEG/PNG等)を想定したものであり、`mode:'bulk'`(PDF入力)をそのまま`image_url`のdata URIとして渡せる保証が無い(Anthropicの`document`ブロックのような「PDFをそのまま渡せる」専用形式が、OpenAI Chat Completions APIには存在しない可能性がある)。ライブ検証時に、PDF→画像変換や別APIの要否を確認する必要がある。この制約はコード内コメント(`lib/ai/providers/openai/client.ts`)にも明記済み。
3. **Geminiのモデル名(`gemini-2.5-flash`)・OpenAIのモデル名(`gpt-4o`)は、本セッション時点で妥当と考えられる名称を仮置きしたもの**であり、ライブ検証時に実際に提供されているモデル名と照合・必要なら差し替えが必要。
4. **`confidence`(信頼度)はAIモデルの自己申告値であり、統計的にキャリブレーションされた確率ではない**。UIでの「要確認」の目安としては妥当だが、数値そのものを正確な確率として扱ってはならない(コード内コメント・本報告書の両方に明記)。
5. **`inspectionScheduleSheet`のmax_tokens(single 8000/bulk 24000)は実測に基づかない判断値**。実際の帳票(部屋数・記載密度)でトークン消費量を計測し、次Phaseでチューニングする必要がある。
6. **`npm run typecheck`は、このセッションのサンドボックスでは完全にクリーンにできない**(前述「環境制約について」参照)。`next`パッケージ・`@types/react`が入手不可能なため、Phase7と無関係な既存3ファイルで7件のエラーが残存する。`npm install`可能な通常環境での再確認を推奨。
7. **既存のダミーサンプル経路(`#ocrScheduleScenarioSelect`)をUIに残した**(削除するとPhase3の既存Playwright回帰テストが失敗するため)。本番運用時、実データと紛らわしくないか(「動作確認用サンプル」の表示で区別しているが)、実際のユーザーの目線で再確認することが望ましい。
8. **PDF(`mode:'bulk'`)での実アップロードの実ブラウザ確認は、画像(single)のみで行った**(Playwrightテストでダミーのjpegバイト列を使用。実際のPDFファイルでのブラウザ操作は未確認。ただしサーバー側の`mode:'bulk'`処理自体は単体テストで検証済み)。
9. **レート制限(`enforceRateLimit`)のエンドポイントキー`'inspection-schedule-sheet.scan'`に対する個別の上限値は設定していない**(env既定値を使う。既存の`'inspection-schedule.scan'`と同じ扱い)。運用開始後、利用状況を見て個別調整が必要になる可能性がある。

---

## 次Phase(実運用移行チェックリスト)

設計書の①〜⑦は本Phaseで完了したため、これは新しい「Phase8」の機能追加ではなく、**実運用を開始する前にユーザー側で実施すべき確認事項のチェックリスト**として記載する。

1. **本番/ステージング環境に`ANTHROPIC_API_KEY`を設定**し、実際に撮影した紙の点検予定表の画像で`/api/v1/inspection-schedule-sheet/scan`を呼び、①〜⑥の全パイプライン(OCR→確認画面→テンプレート→Excel書き込み→原紙プレビュー)が実データで動作することを目視確認する(設計書5章⑦の完了条件そのもの)。
2. 上記1で、`roomNumberRaw`等のフィールドが期待通りに埋まるか、`confidence`が実際の読み取り精度とどの程度相関するかを確認し、必要ならプロンプト(`lib/ai/capabilities/ocr/documentTypes/inspectionScheduleSheet.ts`)を調整する。
3. `max_tokens`(single 8000/bulk 24000)が実際の帳票で十分か(出力が途中で切れていないか)を確認し、必要に応じて調整する。
4. `OPENAI_API_KEY`・`GEMINI_API_KEY`を設定し、`TASK_ROUTING['ocr.inspectionScheduleSheet']`を`'openai'`→`'gemini'`と切り替えながら、同じ画像で同じ構造の結果が得られることを確認する(設計書5章⑦の「プロバイダ切り替え」完了条件)。特にOpenAIでのPDF(bulk)入力が実際に機能するかは要注意(前述未対応事項2)。
5. OpenAI/Geminiのモデル名(`gpt-4o`/`gemini-2.5-flash`)が現行の提供モデルと一致しているか確認し、必要なら`lib/ai/providers/{openai,gemini}/client.ts`を更新する。
6. 各プロバイダでの費用(1回あたりのトークン消費量×単価)・レイテンシ(`maxDuration=120`で足りるか)を実測し、必要ならレート制限(`enforceRateLimit`の上限値)を調整する。
7. `npm install`可能な環境(ローカル/CI/Vercel)で`npm run typecheck`・`npm run test:unit`を再実行し、このセッション固有のパッケージ入手性の問題(前述「環境制約について」制約2)が実際には発生しないことを確認する。
8. 本番運用開始後、しばらくは「動作確認用サンプル」経路をUIから削除するか、ユーザーが誤って実データと混同しないよう、さらに視覚的な区別(バッジ等)を強化するか検討する。
